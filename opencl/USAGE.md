# OpenCL GPU Optimizer - Usage Guide

## 🚀 Quick Start

### Using the Automated Script (Recommended)

```bash
# Format: ./run.sh <TICKER> <INTERVAL> [CANDLES]
./run.sh GOOG 1h         # Uses default 600 candles
./run.sh GOOG 1h 1000    # Uses 1000 candles
./run.sh QQQ 4h 800      # Uses 800 candles
./run.sh SPY 1d 500      # Uses 500 candles
```
